document.addEventListener('DOMContentLoaded', function () {
    const ctaBtn = document.querySelector('.cta-btn');
    
    // Ajouter un effet au bouton CTA
    ctaBtn.addEventListener('click', function() {
        aler